# Enhanced Agent Core Dependencies
# Python 3.9+

# ========== Core Dependencies ==========
numpy>=1.21.0

# ========== Vector Memory (NEW) ==========
# ChromaDB for vector storage
chromadb>=0.4.0

# Sentence Transformers for local embeddings (optional but recommended)
sentence-transformers>=2.2.0

# ========== MCP Protocol (NEW) ==========
# MCP SDK
mcp>=1.0.0

# ========== Temporal (NEW) ==========
# Temporal SDK (optional - falls back to file-based if unavailable)
temporalio>=0.10.0

# ========== LLM Integration (optional) ==========
# OpenAI for embeddings and LLM calls
openai>=1.0.0

# Anthropic for Claude integration
anthropic>=0.8.0

# ========== Development ==========
pytest>=7.0.0
pytest-asyncio>=0.21.0
black>=22.0.0
mypy>=1.0.0

# ========== Production Monitoring (optional) ==========
# opentelemetry-api>=1.20.0
# opentelemetry-sdk>=1.20.0
