# Memory Log - 2026-03-12

## ShadowHunter Trading Bot - Working Version Saved

**Time:** 06:17 AM (Asia/Shanghai)

### Summary
User (sshadowfaxx.eth) provided the fully working version of the ShadowHunter wallet tracking bot. The code has been saved to:
- `/root/.openclaw/workspace/trackerv2_working.py`

### Key Fixes in This Version

1. **ATH Scanner Fixed**: 
   - Uses `quantile(of: Trade_PriceInUSD, level: 0.9999)` for accurate ATH calculation
   - Removed problematic `orderBy` syntax that was causing GraphQL errors
   - Fetches 1000 trades and finds max price manually as fallback

2. **Live Cluster Updates**:
   - Sends update on EVERY transaction (buy AND sell)
   - 30-second cooldown between alerts (`CLUSTER_ALERT_COOLDOWN = 30`)
   - Shows ALL wallets that have ever traded the token
   - Displays holding percentage, PnL, SOL invested, time since buy

3. **Fresh Market Cap**:
   - `_fetch_fresh_token_info()` method bypasses cache for alerts
   - Shows real-time prices from DexScreener
   - Falls back to Bitquery for Pump.fun tokens

4. **Token Detection**:
   - Uses `owner` field in `preTokenBalances`/`postTokenBalances`
   - Filters for actual token transfers (not SOL balance changes)
   - Minimum SOL threshold: 0.02 SOL

### Bot Status
- **Status**: FULLY OPERATIONAL
- **Wallets Tracked**: 33 (from wallets.txt)
- **Check Interval**: 5 seconds
- **Channels**: CHANNEL_PINGS (normal), CHANNEL_VIP (clusters), CHANNEL_SCANNER (token scanning)
- **Features**: Token scanner, ATH calculation, wallet stats (Birdeye API), position tracking

### Known Limitations
- Birdeye API rate limit hit on free tier (~100 requests/day)
- Some Pump.fun tokens not indexed by DexScreener (Bitquery fallback handles these)

---
