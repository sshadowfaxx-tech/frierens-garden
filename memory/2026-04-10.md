# Memory Log — 2026-04-10

## Telegram /performance Command — Reverted Fix

**Context:**
- Previous fix (2026-04-08): Escaped special Markdown characters (`_`, `*`, `[`, `]`) in wallet labels to prevent Telegram parsing errors
- User decision (2026-04-08): Reverted the code changes — will instead avoid using special characters in wallet names

**Reverted Changes in `trackerv2_clean.py`:**
1. `_show_performance_leaderboard()` — removed `name_escaped` variable
2. `_show_wallet_performance()` — removed `wallet_label_escaped` variable  
3. `_show_wallet_performance_callback()` — removed `wallet_label_escaped` variable

**Current State:**
- Code is back to original (pre-fix) state
- User will manually ensure wallet labels don't contain: underscores, asterisks, or brackets
- `/performance` command will work as long as wallet naming convention is followed

**Constraint Added:**
Wallet labels must avoid Telegram Markdown special characters to prevent "Can't parse entities" errors.

